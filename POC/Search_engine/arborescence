search_engine/
├── app/
│   ├── tfidf_app.py F                # Streamlit UI pour le moteur TF-IDF
│   ├── embedding_app.py F            # Streamlit UI pour le moteur sémantique
│   └── common_ui.py                # Code Streamlit commun (affichage résultats, surlignage, colonnes...)
│
├── core/
│   ├── data_loader.py              # Chargement JSON et pré-traitement
│   ├── vectorizer_tfidf.py         # Vectorisation TF-IDF + recherche
│   ├── vectorizer_embedding.py     # Embedding LangChain + FAISS
│   └── utils.py                    # Fonctions utilitaires (surlignage, similarité, etc.)
│
├── preprocess/
│   ├── processing_query.py         # Preprocessing requête classique
│   └── test_preprocessed_emb.py    # Preprocessing spécifique pour les embeddings
│
├── config/
│   ├── paths.py                    # Tous les chemins vers les fichiers centralisés ici
│   └── settings.py                # Paramètres (top_k, modèles, normalisation, etc.)
│
├── index/
│   └── (stocke l''index FAISS ou tout autre vecteur)
│
├── data/
│   └── (stocke les JSON de données pré-traitées)
│
├── main.py  F                       # Pour lancer l’app via menu ou auto-switch
└── requirements.txt
